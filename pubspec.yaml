name: matamy
description: مطعمي 100% REAL - FCM حقيقي + Rules حقيقية + Excel Share حقيقي + Exact Fee
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  cloud_firestore: ^4.15.5
  firebase_auth: ^4.16.0
  firebase_storage: ^11.6.0
  firebase_messaging: ^14.7.10
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  image_picker: ^1.0.7
  url_launcher: ^6.2.2
  share_plus: ^7.2.1
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.2
flutter:
  uses-material-design: true
